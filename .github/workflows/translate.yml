# Translation sync — DeepL
#
# Prerequisites — configure these secrets in GitHub repository settings:
#   DEEPL_API_KEY  — DeepL API key (free or pro tier)
#
# On push to main:   translate new/changed English strings and open a PR.
# Manual dispatch:   trigger a full translation sync on demand.

name: Translate — DeepL

"on":
  push:
    branches: [ main ]
    paths:
      - "frontend/src/locales/en/translation.json"
  workflow_dispatch:

jobs:
  translate:
    name: Translate and open PR
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # GITHUB_TOKEN only needs to read the tree. The branch push and the pull
    # request are made with a GitHub App token minted below, because this
    # repository's default workflow permissions are read-only and a workflow
    # cannot grant itself more than the default allows -- declaring
    # `contents: write` here did not confer it, it just made the refusal
    # look like a mystery.
    permissions:
      contents: read
    steps:
      # hardening-exception: egress-audit — this job installs from the npm registry
      #   and clones this repository; harden-runner has never run in this repository,
      #   so no endpoint baseline exists and a block policy written without one fails
      #   closed on its first run; audit is what produces that list
      - name: Harden the runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          egress-policy: audit
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1 # zizmor: ignore[artipacked] -- opens the translations PR

      - name: Mint a token for the translations branch and PR
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: "${{ vars.TRANSLATE_APP_ID }}"
          private-key: "${{ secrets.TRANSLATE_APP_KEY }}"
          # Narrow the token to what this job does, rather than inheriting the
          # installation's blanket permissions (zizmor: dangerous use of GitHub
          # App tokens). Request ONLY permissions the installation actually
          # holds -- asking for one it lacks makes the token step 422, which is
          # how a sibling repo broke release-please and had to revert. Both of
          # these are proven present: the same App pushed the i18n branch and
          # opened the translations PR on a dispatch before this narrowing.
          permission-contents: write
          permission-pull-requests: write

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24

      - name: Translate all locales via DeepL
        run: node scripts/translate.mjs --all
        env:
          DEEPL_API_KEY: ${{ secrets.DEEPL_API_KEY }}

      - name: Create PR with translations
        id: cpr
        # v8.1.1
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8.1.1
        with:
          token: "${{ steps.app-token.outputs.token }}"
          branch: i18n/auto-translate
          title: "chore(i18n): update translations"
          body: |
            Auto-generated by the translation workflow (DeepL).

            **Review checklist:**
            - [ ] Spot-check a few strings in each language for accuracy
            - [ ] No English source strings appear verbatim in non-English files
            - [ ] Interpolation tokens (`{{name}}`) are preserved correctly
          # This message carried a CI-skip directive from the day the workflow was
          # written. GitHub honours those on a branch's head commit for BOTH the push
          # and the pull_request event, so every PR this job opened was created with
          # zero check runs and zero commit statuses -- every required context stuck on
          # "Expected - waiting for status", clearable only by an admin override.
          # Twelve translation PRs merged that way, none ever inspected by a gate.
          #
          # The head commit of this branch never changes after creation, so the
          # suppression lasted the whole life of each PR. The only reason CI was ever
          # seen on this branch is that "Update branch" writes a merge commit whose
          # message has no directive -- those five head commits have 6 to 10 checks.
          #
          # Removing it cannot loop: this job triggers on pushes that touch
          # frontend/src/locales/en/translation.json, and scripts/translate.mjs only
          # ever writes the nine non-English bundles and scripts/.translation-hashes.json.
          # Merging its own PR therefore does not re-trigger it. The recursion the
          # directive appeared to guard against was never possible.
          #
          # scripts/check-workflow-ci-skip.mjs fails CI if the directive comes back,
          # in any spelling: it tests the resolved YAML value, not the source text.
          commit-message: "chore(i18n): update translations"
          delete-branch: true

      # Zero-latency half of the guard. The scheduled auditor in pr-ci-presence.yml is
      # the suppression-immune backstop, but it runs on a cadence and one of these PRs
      # was merged within the hour. This asserts, in the run that opens the PR, that
      # checks actually attached to it -- and this job is reachable, because it is
      # triggered by a human push to main rather than by the bot commit under test.
      #
      # Read-only and unauthenticated-capable: check runs are world-readable on this
      # public repository, so no additional App permission is requested. Asking the App
      # for a permission its installation lacks makes the token step 422 -- that is what
      # broke release-please in a sibling repository and had to be reverted.
      - name: The translation PR must have CI attached
        if: >-
          steps.cpr.outputs.pull-request-operation == 'created' ||
          steps.cpr.outputs.pull-request-operation == 'updated'
        env:
          # Through env, not interpolated into the script line: a `${{ }}` expansion
          # inside a run block is executed by the shell, which is the template-injection
          # class this repository's workflow-security gate rejects.
          GITHUB_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.cpr.outputs.pull-request-number }}
        run: node scripts/assert-pr-checks-present.mjs --pr "$PR_NUMBER" --wait 10
