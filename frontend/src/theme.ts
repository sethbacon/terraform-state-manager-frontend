// Re-exported from the shared suite package; the local path is kept for existing imports.
export { createAppTheme, BRAND_PRIMARY } from '@4cloudguru/cloud-suite-ui'
export type { ThemeMode, ThemeOverrides } from '@4cloudguru/cloud-suite-ui'

