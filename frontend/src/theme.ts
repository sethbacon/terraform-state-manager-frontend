// Re-exported from the shared suite package; the local path is kept for existing imports.
export { createAppTheme, BRAND_PRIMARY } from '@sethbacon/terraform-suite-ui'
export type { ThemeMode, ThemeOverrides } from '@sethbacon/terraform-suite-ui'

