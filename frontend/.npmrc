legacy-peer-deps=true
# Without this npm only WARNS on an engines mismatch and builds anyway, which is
# how the builder image silently ran two majors ahead of `engines` for weeks (#255).
engine-strict=true
